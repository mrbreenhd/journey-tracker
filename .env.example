# Journey Tracker — environment configuration
# Copy to .env and fill in the values

DATABASE_URL=postgresql+asyncpg://localhost:5432/journey_tracker
MAPBOX_TOKEN=
UPLOAD_DIR=backend/uploads
ALLOWED_ORIGINS=["*"]

# Frontend (also needs VITE_ prefix for Vite)
VITE_MAPBOX_TOKEN=
